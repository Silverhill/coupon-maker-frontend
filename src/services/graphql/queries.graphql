query getHunters {
  allHunters {
    name
  }
}

query getMe {
  me {
    name
    _id
    role
  }
}

mutation loginUser($email: String!, $password: String!) {
  login(email: $email, password: $password)
}