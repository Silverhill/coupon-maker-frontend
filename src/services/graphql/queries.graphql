query getHunters {
  allHunters {
    name
  }
}

query getMe {
  me {
    name
    _id
    role
  }
}

query loginUser($email: String!, $password: String!) {
  signIn(email: $email, password: $password) {
   token
  }
}
